pragma solidity ^0.5.0;
contract HelloDeposit {
address owner;
uint8 peopleCount;

constructor()public {owner = msg.sender;
}
function finalize(address addr)public payable {if (totalAmount ==10.0){ 
addr.transfer(this.balance);
}
function join()public payable {require (peopleCount < 2);
peopleCount++;
require(msg.value ==5.0);
}

}