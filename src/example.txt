pragma solidity ^0.5.0;
contract HelloDeposit {
constructor() public {owner = msg.sender;
}
function finalize() public payable payable {if (totalAmount ==10.0){ 
owner.transfer(this.balance);
}
function join() public payable payable {require (peopleCount < 2);
peopleCount++;
require(msg.value ==5.0);
}

}